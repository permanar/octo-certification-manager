name: colleger_repository

version: 1.0.0+1

environment:
  sdk: ">=2.2.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cloud_firestore: 
  rxdart: ^0.22.0
  equatable: ^1.0.1
  firebase_core: ^0.4.0+7
